name: 每月更新港鐵巴士資料

on:
  schedule:
    # 每月 1 號 UTC 17:00 = 香港時間 01:00
    - cron: '0 17 1 * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 安裝依賴
        run: npm install

      - name: 執行更新腳本
        run: npm run update-data

      - name: 提交更新
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || git commit -m "🤖 每月自動更新港鐵巴士資料"
          git push
